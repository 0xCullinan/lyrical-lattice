apiVersion: v1
kind: Secret
metadata:
  name: oronym-secrets
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual values in production
  # These are placeholders and should NEVER be committed to version control
  database-url: "postgresql+asyncpg://user:password@postgres:5432/oronym_db"
  redis-url: "redis://redis:6379/0"
  genius-api-key: "your-genius-api-key"
